# SECURITY CONFIGURATION - CHANGE ALL THESE VALUES!

# Django Security
SECRET_KEY=generate-a-long-random-string-here
DEBUG=False
ALLOWED_HOSTS=YOUR_VPS_IP,127.0.0.1,localhost

# CORS (for IP-based access during testing)
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=

# Admin Credentials - CHANGE THESE!
ADMIN_USERNAME=youradminuser
ADMIN_PASSWORD=your-very-strong-admin-password

# Database Configuration
DB_NAME=resolwe
DB_USER=resolwe
DB_PASSWORD=your-strong-database-password
DB_HOST=localhost
DB_PORT=5432

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1

# SSL/Security (set to True when using HTTPS)
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# PgAdmin (only if using Docker Compose)
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=another-strong-password

# Optional: Resolwe URL for scripts
RESOLWE_URL=http://YOUR_VPS_IP
